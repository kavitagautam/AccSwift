import { TimezoneDirective } from './timezone.directive';

describe('TimezoneDirective', () => {
  it('should create an instance', () => {
    const directive = new TimezoneDirective();
    expect(directive).toBeTruthy();
  });
});
