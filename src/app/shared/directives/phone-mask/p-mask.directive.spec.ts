import { PMaskDirective } from './p-mask.directive';

describe('PMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new PMaskDirective();
    expect(directive).toBeTruthy();
  });
});
