import { PreferredDateFormatPipe } from './preferred-date-format.pipe';

describe('PreferredDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PreferredDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
